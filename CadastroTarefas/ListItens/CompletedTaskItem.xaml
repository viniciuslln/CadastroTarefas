<Border
    x:Class="CadastroTarefas.ListItens.CompletedTaskItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CadastroTarefas.ListItens"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Margin="8"
    d:DesignHeight="200"
    d:DesignWidth="200"
    BorderBrush="Gray"
    BorderThickness="3"
    CornerRadius="3"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.ColumnSpan="3"
            Padding="8"
            Text="{Binding Description, FallbackValue=None}"
            TextWrapping="Wrap" />
        <Button
            Grid.Row="1"
            Grid.Column="2"
            Padding="8"
            Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
            CommandParameter="{Binding .}"
            Content="Remover" />

    </Grid>
</Border>
